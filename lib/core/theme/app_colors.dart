import 'dart:ui';

class AppColors {
  AppColors._();

  // Background
  static const Color deepSapphire = Color(0xFF002366);
  static const Color deepSapphireDark = Color(0xFF001233);

  // Accent
  static const Color glowingBlue = Color(0xFF2B6CEE);
  static const Color glowingBlueSoft = Color(0x552B6CEE);

  // Glass
  static const Color glassWhite = Color(0x40FFFFFF); // 25%
  static const Color glassBorder = Color(0x66FFFFFF); // 40%
  static const Color glassHighlight = Color(0x1AFFFFFF); // 10%

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFE0E0E0);
  static const Color textMuted = Color(0xAAFFFFFF);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFEF5350);
}
